#Strucutre where we can store multiple picses of information. 
coordinates = (4, 5) #Tuples with coordinates in it. #Are immutable - cant be changed or modified.
coordinates[1] = 10 #Cannot change the tuple assignment.
print(coordinates[1])

#list are doing the same thing, but Tuples are immutable as you cannot modify elements in tuple like you can with list.